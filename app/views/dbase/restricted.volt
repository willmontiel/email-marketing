{{ content() }}
<div class="row-fluid">
    <div class="modal-header text-error">
        <h1>ACCESO RESTRINGIDO!</h1>
    </div>
    <span><a href="{{ url('emarketing/dbase') }}">Regresar a la Base de Datos</a></span>
</div>